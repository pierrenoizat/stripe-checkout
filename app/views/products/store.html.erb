<h3>Select your favorite, then pay by Bitcoin or Card</h3>

<div class="container-fluid">
	<div class="row" >
	<% @products.each_slice(4) do |products_for_row| %>
		<div class="col-md-12" >
		<% products_for_row.each do |product| %>
			<div class="col-md-3" >

			<%= render product %>

			</div>
		<% end %>
		</div></br></br>
	<% end %>
	</div>
</div>